import { MapListDirective } from './map-list.directive';

describe('MapListDirective', () => {
  it('should create an instance', () => {
    const directive = new MapListDirective();
    expect(directive).toBeTruthy();
  });
});
